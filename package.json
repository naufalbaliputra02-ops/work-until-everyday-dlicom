{
  "name": "work-until-everyday-dlicom",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "uv run python tools/build.py",
    "test": "node --test \"game/test/*.test.js\"",
    "start": "python3 -m http.server ${PORT:-3000} --directory game"
  }
}
